'use client';

import React from 'react';

export default function Stock() {
  return (
    <div className="p-4">
      <h1 className="font-semibold text-lg">Stock — Skeleton OK</h1>
      <p>If you can see this page after deploy, the build pipeline is fine.</p>
    </div>
  );
}
